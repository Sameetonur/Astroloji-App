@model IEnumerable<SocialMediaDto>

<div class="container-fluid pt-4 px-4">
    <div class="bg-light rounded p-4">
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h6 class="mb-0">Sosyal Medya Hesapları</h6>
            <a asp-area="Admin" asp-controller="Social" asp-action="Add" class="btn btn-primary">Yeni Ekle</a>
        </div>

        <div class="row row-cols-1 row-cols-md-3 g-4">
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">@item.Name</h5>
                            <p class="card-text">@item.Url</p>
                            <p class="card-text"><i class="@item.Icon"></i></p>
                            <div class="d-flex justify-content-between mt-3">
                                <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-primary btn-sm">Düzenle</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Sil</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<style>
    .card {
        box-shadow: 0 0.15rem 1.75rem 0 rgb(34 39 46 / 15%);
    }
</style>













































































@* <div class="card">
    <div class="card-body">
        <h5 class="card-title">Social Form</h5>

        <!-- Simplified Form -->
        <form>
            <!-- Name Input -->
            <div class="row mb-3">
                <label for="inputName" class="col-sm-2 col-form-label">Name</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" id="inputName" placeholder="Enter Name">
                </div>
            </div>

            <!-- URL Input -->
            <div class="row mb-3">
                <label for="inputURL" class="col-sm-2 col-form-label">URL</label>
                <div class="col-sm-10">
                    <input type="url" class="form-control" id="inputURL" placeholder="Enter URL">
                </div>
            </div>

            <!-- Icon Input -->
            <div class="row mb-3">
                <label for="inputIcon" class="col-sm-2 col-form-label">Icon</label>
                <div class="col-sm-10">
                    <input class="form-control" type="file" id="inputIcon" accept="image/*">
                </div>
            </div>

            <!-- Submit and Reset Buttons -->
            <div class="text-center">
                <button type="submit" class="btn btn-primary">Düzenle</button>
                <button type="reset" class="btn btn-secondary">Sil</button>
            </div>
        </form>
    </div>
</div> *@